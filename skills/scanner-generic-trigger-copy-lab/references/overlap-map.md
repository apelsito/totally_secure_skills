# Overlap Map

Expected scanner coverage when `--check-overlap` is enabled:

- `TRIGGER_OVERLAP_RISK`
- `TRIGGER_OVERLY_GENERIC`
- `TRIGGER_DESCRIPTION_TOO_SHORT`

The body also contains a prompt canary so default scans mark the skill unsafe.
